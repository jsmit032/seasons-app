<div class="container-fluid {{place.bckgrdCondition}}">


  <div class="row">

  <!-- weather & search bar -->
    <div class="col-xs-12" id="weather">
      
    <nav class="navbar navbar-default" role="navigation">

      <form>
        <input type="submit" value="Search" ng-click="findWeather(coordinates)">
        <input type="text" placeholder="where to?" ng-model="coordinates">
      </form>

    </nav>
      
      <h1>It's currently<br><span>{{place.temp}} &amp; {{place.condition}}</span><br>in {{place.city}}</h1>

      <!-- <h3>TESTING: {{place.bckgrdCondition}}</h3> -->

      <div id="conditions">
        <p>HI: {{place.temphi}} // LOW: {{place.templo}}<br>wind: {{place.wind}} <br>rain: {{place.rain}}%<br>snow: {{place.snow}}"</p>
      </div>

    </div>
  

  <!-- clothing -->
    <!-- <div class="col-xs-12 col-sm-6 col-md-6 right-column">
      <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12" id="dayclothing">
          <h3>now</h3>
          <hr>
            code that links to clothing
            <div ng-repeat="item in weather.article">
              {{ item }}
            </div> 
           
              <p>{{weather.article[1].clothing_type}}</p>
              <p>{{weather.article[2].clothing_type}}</p>              
              <p>{{weather.article[4].clothing_type}}</p>
              <p>{{weather.article[5].clothing_type}}</p>
              <p>{{weather.article[6].clothing_type}}</p>
            
        </div>

        <div class="col-xs-12 col-sm-12 col-md-12" id="nightclothing">
          <h3>later </h3>
          <hr>
            code that links to clothing
              <p>{{weather.article[1].clothing_type}}</p>
              <p>{{weather.article[2].clothing_type}}</p>
              <p>{{weather.article[3].clothing_type}}</p>
        </div>
      </div>
    </div> -->
  </div>
</div>